CF_API_TOKEN=your_token_here

# 支援多域名與多 Zone ID
# 格式 1 (推薦): domain1:zone1, domain2:zone2
# 格式 2: domain1, domain2 (這時會共用下方設定的單一 ZONE_ID)
RECORD_NAME=pi.example.com:zone_id_1, home.another.com:zone_id_2

# 如果只用單一 Zone，可以在這裡設定
ZONE_ID=

CHECK_INTERVAL=300
DOCKER_MODE=false
